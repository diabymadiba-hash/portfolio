const navbar = document.getElementById("navbar"); window.addEventListener("scroll", () => { navbar.classList.toggle("scrolled", window.scrollY > 20) }); const navToggle = document.getElementById("navToggle"), navMenu = document.querySelector(".nav-liens"); navToggle.addEventListener("click", () => { navToggle.classList.toggle("open"), navMenu.classList.toggle("open") }), navMenu.querySelectorAll("a").forEach(e => { e.addEventListener("click", () => { navToggle.classList.remove("open"), navMenu.classList.remove("open") }) }); const sections = document.querySelectorAll("section[id]"), navLinks = document.querySelectorAll(".nav-liens a"); window.addEventListener("scroll", () => { let e = ""; sections.forEach(t => { window.scrollY >= t.offsetTop - 100 && (e = t.id) }), navLinks.forEach(t => { t.classList.toggle("active", t.getAttribute("href") === "#" + e) }) }); const reveals = document.querySelectorAll(".reveal"), revealObserver = new IntersectionObserver(e => { e.forEach(e => { e.isIntersecting && (e.target.classList.add("visible"), revealObserver.unobserve(e.target)) }) }, { threshold: .12 }); reveals.forEach(e => revealObserver.observe(e)); const grillePhares = document.querySelector(".grille-projets-phares"), grilleAutres = document.querySelector(".grille-projets"), delaisReveal = ["", "delay-1", "delay-2", "delay-3"]; function carteProjetPhareHTML(e, t) { return `\n    <a href="${e.lienDetail}" class="carte-projet carte-projet-phare reveal ${t}">\n      <div class="carte-image ${e.gradientClass}">\n        <span class="badge-phare">★ Projet phare</span>\n        <img src="${e.image}" alt="${e.imageAlt}">\n        <div class="carte-survol">\n          <span class="survol-btn survol-btn-bleu">Voir le projet complet</span>\n        </div>\n      </div>\n      <div class="carte-contenu">\n        <div class="carte-tags">\n          ${e.tags.map(e => `<span class="carte-tag">${e}</span>`).join("")}\n        </div>\n        <h3 class="carte-titre">${e.titre}</h3>\n        <p class="carte-texte">${e.description}</p>\n        <div class="carte-pied">\n          <span class="carte-lien">Voir le détail →</span>\n        </div>\n      </div>\n    </a>` } function carteProjetHTML(e, t) { return `\n    <a href="${e.lienLive}" target="_blank" rel="noopener" class="carte-projet reveal ${t}">\n      <div class="carte-image carte-image-statique ${e.gradientClass}">\n        <img src="${e.image}" alt="${e.imageAlt}">\n      </div>\n      <div class="carte-contenu">\n        <div class="carte-tags">\n          ${e.tags.map(e => `<span class="carte-tag">${e}</span>`).join("")}\n        </div>\n        <h3 class="carte-titre">${e.titre}</h3>\n        <p class="carte-texte">${e.description}</p>\n        <div class="carte-pied">\n          <span class="carte-lien">Voir le site →</span>\n        </div>\n      </div>\n    </a>` } async function chargerProjets() { try { const e = await (await fetch("data/projets.json")).json(), t = e.filter(e => e.phare), r = e.filter(e => !e.phare); grillePhares.innerHTML = t.map((e, t) => carteProjetPhareHTML(e, delaisReveal[t] || "")).join(""), grilleAutres.innerHTML = r.map((e, t) => carteProjetHTML(e, delaisReveal[t] || "")).join(""), document.querySelectorAll("#projects .reveal").forEach(e => revealObserver.observe(e)) } catch (e) { console.error("Erreur de chargement des projets :", e) } } chargerProjets(), document.querySelectorAll(".barre-niveau[data-level]").forEach(e => { e.style.width = e.dataset.level + "%" }); const scrollTopBtn = document.getElementById("scrollTop"); window.addEventListener("scroll", () => { scrollTopBtn.classList.toggle("visible", window.scrollY > 400) }), scrollTopBtn.addEventListener("click", () => window.scrollTo({ top: 0, behavior: "smooth" })); const form = document.getElementById("contactForm"), formStatus = document.getElementById("formStatus"); function showStatus(e, t) { formStatus.textContent = e, formStatus.className = "form-statut form-statut-" + t } form.addEventListener("submit", async e => { e.preventDefault(); const t = document.getElementById("fname").value.trim(), r = document.getElementById("lname").value.trim(), a = document.getElementById("email").value.trim(), s = document.getElementById("subject").value, n = document.getElementById("message").value.trim(); if (!(t && r && a && s && n)) return void showStatus("Veuillez remplir tous les champs obligatoires.", "err"); if (!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(a)) return void showStatus("Veuillez entrer une adresse email valide.", "err"); const l = form.querySelector(".form-submit"); l.textContent = "Envoi en cours…", l.disabled = !0; try { (await fetch(form.action, { method: form.method, body: new FormData(form), headers: { Accept: "application/json" } })).ok ? (showStatus("✅ Message envoyé !", "ok"), form.reset()) : showStatus("Une erreur est survenue, veuillez réessayer.", "err") } catch { showStatus("Une erreur est survenue, veuillez réessayer.", "err") } finally { l.innerHTML = '<i class="fa-solid fa-paper-plane" aria-hidden="true"></i> Envoyer le message', l.disabled = !1 } });